{
  "name": "terraform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "terraform:init": "terraform init",
    "terraform:plan": "./node_modules/.bin/dotenv -- terraform plan",
    "terraform:apply": "./node_modules/.bin/dotenv -- terraform apply",
    "terraform:refresh": "./node_modules/.bin/dotenv -- terraform refresh",
    "terraform:output": "terraform output",
    "terraform:graph": "./node_modules/.bin/dotenv -- terraform graph | dot -Tsvg > graph.svg"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv-cli": "^4.0.0"
  }
}
