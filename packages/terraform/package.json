{
  "name": "terraform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "terraform:go:dev": "cd ./main/dev",
    "terraform:dev:init": "npm run terraform:go:dev && terraform init",
    "terraform:dev:plan": "cd ./main/dev && ../../node_modules/.bin/dotenv -e ../../.env -- terraform plan",
    "terraform:dev:apply": "npm run terraform:go:dev && ../../node_modules/.bin/dotenv -- terraform apply",
    "terraform:dev:refresh": "npm run terraform:go:dev && ../../node_modules/.bin/dotenv -- terraform refresh",
    "terraform:dev:output": "npm run terraform:go:dev && terraform output"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv-cli": "^4.0.0"
  }
}
