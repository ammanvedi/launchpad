export const log = {
    error: (msg: string) => {
        console.log(`ERROR :: ${msg}`);
    },
    info: (msg: string) => {
        console.log(`INFO :: ${msg}`);
    },
    success: (msg: string) => {
        console.log(`SUCCESS :: ${msg}`);
    },
};
