{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:generate": "./node_modules/.bin/prisma generate",
    "prisma:save-migration": "./node_modules/.bin/prisma migrate save --experimental",
    "prisma:apply-migration": "./node_modules/.bin/prisma migrate up --experimental",
    "prisma:apply-schema": "npm run prisma:save-migration && npm run prisma:apply-migration"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^2.10.1",
    "@types/node": "^14.14.6",
    "typescript": "^4.0.5"
  },
  "devDependencies": {
    "@prisma/cli": "^2.10.1"
  }
}
