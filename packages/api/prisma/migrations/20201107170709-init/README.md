# Migration `20201107170709-init`

This migration has been generated by ammanvedi at 11/7/2020, 5:07:09 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."Role" AS ENUM ('USER', 'DESIGNER', 'MANUFACTURER', 'DESIGNER_MANUFACTURER', 'PARTNER')

CREATE TABLE "public"."User" (
"id" text   NOT NULL ,
"email" text   NOT NULL ,
"firstName" text   ,
"lastName" text   ,
"profileImage" text   ,
"bio" text   ,
"role" "Role"  NOT NULL DEFAULT E'USER',
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201107170709-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,29 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+enum Role {
+    USER
+    DESIGNER
+    MANUFACTURER
+    DESIGNER_MANUFACTURER
+    PARTNER
+}
+
+model User {
+  id            String      @id
+  email         String      @unique
+  firstName     String?
+  lastName      String?
+  profileImage  String?
+  bio           String?
+  role          Role        @default(USER)
+}
```


